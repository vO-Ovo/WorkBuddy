# 部署指南：获取「永久稳定 + 自动更新」的访问链接

> 适用对象：标书专员 / 团队负责人
> 目标：让网页以后**随时能打开、链接永远不变、内容始终是最新**，且**基本不花钱**。

---

## 一、为什么现在的链接会失效（根因）

当前用的部署工具（CloudStudio）**每次部署都会生成一个新沙箱、分配一个新 URL**；旧沙箱空闲后被回收，旧链接就打不开了。

网站本身没问题（数据已覆盖广东省 **21 个地级市、164 条真实公告**），问题纯粹出在「承载它的地址一直在变」。

**解法**：换成「固定域名的静态托管 + Git 自动部署」。

---

## 二、推荐方案：腾讯云 EdgeOne Pages（免费额度内 0 元）

| 维度 | 说明 |
|---|---|
| 收费吗 | 有**永久免费版**（`Free $0`）；静态站每月 **10GB CDN 流量免费**。本站点量极小，**基本不花钱**；仅当流量远超免费额度才计费（几乎不可能触发）。 |
| 链接会变吗 | **不会**。绑定 Git 仓库后，每次 `git push` 自动部署，域名（如 `https://<项目名>.edgeone.app`）**永久不变**。 |
| 国内速度 | 腾讯云 2500+ 边缘节点加速，国内秒开。 |
| 怎么做到「最新」 | 每日 09:00 / 15:00 自动化把新公告写进 `js/data.js` 后自动 `git push`，EdgeOne 自动拉取并刷新网站。 |

备选：GitHub Pages（免费但国内常慢/需代理）、Vercel/Netlify（免费但国内一般）、自建服务器（要钱+运维，不推荐）。

---

## 三、准备工作（你来做，约 5 分钟）

1. **注册一个 Git 账号**（任选其一）：
   - **GitHub**：https://github.com （EdgeOne 支持最完善；国内推送偶尔需代理）
   - **Gitee 码云**：https://gitee.com （国内访问/推送更顺畅）
2. 在 Git 平台**新建一个空仓库**（仓库名随意，如 `medical-tender-gd`），**不要**勾选「自动生成 README / .gitignore」，保持空仓库以便首次推送。
3. 复制仓库的 **HTTPS 地址**，形如：
   - `https://github.com/你的用户名/medical-tender-gd.git`
   - 或 `https://gitee.com/你的用户名/medical-tender-gd.git`

---

## 四、首次部署（你来做）

1. 打开腾讯云 EdgeOne Pages：https://edgeone.cloud.tencent.com/ → 进入「**Pages**」。
2. 点击「**新建应用 / 导入 Git 仓库**」→ 授权 GitHub 或 Gitee（按提示登录并授权）。
3. 选择刚建的仓库，分支选 `main`。
4. **构建设置**（关键）：
   - 框架预设：选「**无 / 静态网站**」（或 None）
   - 构建命令：**留空**（纯静态，无需构建）
   - 输出目录：`.`（根目录，因为 `index.html` 就在根）
5. 点击「**开始部署**」，等待 1–2 分钟。完成后会生成一个**固定域名**（如 `https://medical-tender-gd.edgeone.app`）——这就是你以后随时打开的链接，**不会再变**。
6. （可选）在「域名设置」里绑定你自己的域名，EdgeOne 会自动签发 SSL 证书（HTTPS）。

---

## 五、日常自动更新（已为你配置好）

- 项目已初始化为 Git 仓库，本地代码已提交。
- 定时自动化（每日 **09:00 / 15:00**）在抓取到新公告并写入 `js/data.js`、通过数据质量门禁后，会自动执行：
  ```bash
  git add js/data.js
  git commit -m "chore: 自动扩充招标公告（DATA_UPDATED=YYYY-MM-DD）"
  git push
  ```
- 推送后 EdgeOne **自动重新部署**，你的固定链接内容始终是最新的，无需任何手动操作。

### 你只需补一步：把本地仓库连到远程（做一次即可）

在 WorkBuddy 本机终端，于项目目录执行：

```bash
cd "粤东医疗设备招标信息汇总平台"
git remote add origin https://github.com/你的用户名/medical-tender-gd.git
git branch -M main
git push -u origin main
```

之后每天的自动化会自动 push，你无需再管。

---

## 六、常见问题

- **Q：页面样式错位 / 没样式？**
  A：站点用 Tailwind CSS CDN，首次打开需联网；请确保网络可访问 `cdn.tailwindcss.com`。

- **Q：推送后页面没变？**
  A：EdgeOne 默认有缓存，等 1–2 分钟；或在 EdgeOne 控制台点「重新部署」。站点对 `js/data.js` 带 `?t=时间戳` 缓存击穿，数据更新会立即生效。

- **Q：想完全离线、不依赖 Tailwind CDN？**
  A：可把 Tailwind 下载到本地 `assets/` 并改 `index.html` 引用，另请我处理即可。

- **Q：收费吗？**
  A：免费额度内 **0 元**；只有流量远超 10GB/月才计费（内部小站几乎不可能）。

- **Q：链接安全吗？能被外人看到吗？**
  A：固定域名默认公开可访问。若仅内部使用，可在 EdgeOne 配置访问密码 / 防盗链，或绑定内网域名；需要的话我帮你加。
