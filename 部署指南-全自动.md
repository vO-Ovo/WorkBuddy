# 全自动部署指南 — 无需打开 WorkBuddy 即可访问

> **目标**：每天 9:00 与 15:00 自动抓取广东省医疗设备招标公告、自动部署到 EdgeOne Pages，
> 你只需打开浏览器访问固定 URL，所有数据自动保持最新。
> **完全脱离 WorkBuddy**——所有自动化由 GitHub Actions 在云端执行。

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│  GitHub Actions (云端定时任务，每天 9:00/15:00)                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────────┐  │
│  │ 数据质量门禁  │ →  │ 抓取最新公告  │ →  │ git commit/push  │  │
│  └──────────────┘    └──────────────┘    └──────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
              ┌────────────────────────────────┐
              │  GitHub 仓库（公开/私有均可）   │
              └────────────────────────────────┘
                              ↓ 推送触发自动部署
              ┌────────────────────────────────┐
              │  腾讯云 EdgeOne Pages           │
              │  固定 URL：https://xxx.edgeone.app │
              └────────────────────────────────┘
                              ↓
              ┌────────────────────────────────┐
              │  用户（PC/手机浏览器直接访问）  │
              └────────────────────────────────┘
```

---

## 二、首次部署（一次性，约 10 分钟）

### 第 1 步：创建 GitHub 账号 + 仓库（2 分钟）

- 访问 https://github.com/signup 注册免费账号
- 登录后点击右上角「+」→「New repository」
- 填写：
  - **Repository name**：`medical-tender-gd`（可自定义）
  - **Visibility**：Public（推荐，Actions 完全免费）或 Private（Actions 每月 2000 分钟免费，本任务约 60 分钟/月）
- **不要**勾选「Add a README file」「Add .gitignore」「Choose a license」（保持空仓库）
- 点击「Create repository」
- 复制仓库的 HTTPS 地址，形如 `https://github.com/你的用户名/medical-tender-gd.git`

### 第 2 步：推送本地代码到 GitHub（2 分钟）

打开 WorkBuddy 本机终端（项目所在目录）：

```bash
cd "粤东医疗设备招标信息汇总平台"
git remote add origin https://github.com/你的用户名/medical-tender-gd.git
git branch -M main
git push -u origin main
```

推送成功后会看到：
```
* [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

### 第 3 步：在 EdgeOne Pages 创建应用（3 分钟）

- 访问 https://edgeone.cloud.tencent.com/ → 点击「Pages」（首次需要登录腾讯云账号，免费注册）
- 点击「**新建应用 / 导入 Git 仓库**」
- 授权 GitHub → 选择 `medical-tender-gd` 仓库 → 分支选 `main`
- **构建设置**（关键）：
  - 框架预设：**静态网站 / None**
  - 构建命令：**留空**（纯静态，无需构建）
  - 输出目录：`.`（根目录，因为 index.html 在根）
- 点击「开始部署」→ 等待 1-2 分钟
- 部署成功后会显示一个**固定 URL**，形如：
  ```
  https://medical-tender-gd.edgeone.app
  ```
  这就是你以后随时打开的链接，**永远不会变**。

### 第 4 步：验证与使用

- 在浏览器（PC 或手机）打开 `https://medical-tender-gd.edgeone.app`
- 应该看到 164 条公告、21 个城市的广东省医疗设备招标信息汇总平台
- 接下来每天 9:00 与 15:00，**无需任何操作**，数据会自动更新

---

## 三、自动更新机制详解

**触发器**：`.github/workflows/daily-update.yml`（已随代码推送到 GitHub）

**定时**：北京时间每天 **9:00** 与 **15:00**（GitHub Actions cron 使用 UTC，对应 `0 1 * * *` 与 `0 7 * * *`）

**执行流程**：
1. **检出代码**：`git checkout` 最新版本
2. **配置环境**：Node.js 20
3. **数据质量门禁**：`tests/validate-data.cjs` 校验现有数据完整性（防脏数据）
4. **抓取最新公告**：`scripts/scrape.cjs` 调用 ccgp.gov.cn + gdgpo.czt.gd.gov.cn，按城市+设备类型过滤新公告
5. **更新数据文件**：写入 `js/data.js`，更新 `DATA_UPDATED` 时间戳
6. **提交推送**：仅当有新数据时 `git commit + push`
7. **EdgeOne Pages 自动部署**：监听 GitHub 推送，1-2 分钟内重新部署

**手动触发**：在 GitHub 仓库页面 → Actions → 左侧「Daily Data Update & Deploy」→ 右侧「Run workflow」→ 点绿色按钮即可立即跑一次

---

## 四、完整文件清单

### 新增（本指南配套）

| 文件 | 作用 |
|---|---|
| `scripts/scrape.cjs` | 自动抓取 ccgp/gdgpo 公告的 Node.js 脚本（零第三方依赖） |
| `.github/workflows/daily-update.yml` | GitHub Actions 定时工作流（每天 9:00/15:00） |
| `部署指南-全自动.md` | 即本文档 |

### 已有（无需改动）

| 文件 | 作用 |
|---|---|
| `index.html` + `src/*.js` | 响应式前端（PC + 手机自适应） |
| `js/data.js` | 数据文件（被脚本自动更新） |
| `tests/validate-data.cjs` | 数据质量门禁 |
| `assets/styles.css` | 自定义样式（含玻璃态、动画等） |
| `package.json` | npm 脚本与依赖 |

---

## 五、PC + 手机自适应说明

前端使用 **TailwindCSS 响应式设计**（`index.html` 通过 `<script src="https://cdn.tailwindcss.com">` 引入），已实现：

| 设备 | 宽度 | 布局 |
|---|---|---|
| 手机 | < 768px | 单栏堆叠、抽屉式导航、触控优化 |
| 平板 | 768-1023px | 双栏 + 折叠菜单 |
| 桌面 | ≥ 1024px | 左侧导航 + 右侧列表（含分页、弹窗） |

**加载速度优化**：
- Tailwind 通过 CDN JIT 编译，按需生成 CSS，首屏 < 100KB
- 数据通过动态 `<script>` 注入（带 `?t=时间戳` 缓存击穿），更新即时可见
- 所有静态资源走 EdgeOne 全球 CDN（含中国大陆节点），**首屏 < 1 秒**

---

## 六、费用明细

| 项目 | 费用 |
|---|---|
| GitHub 账号 + 公开仓库 | **免费**，无限协作 |
| GitHub Actions（公开仓库） | **免费**，无限时长 |
| GitHub Actions（私有仓库） | 免费 **2000 分钟/月**（本任务每月约 60 分钟） |
| 腾讯云 EdgeOne Pages 免费版 | **免费**，含 10GB/月 CDN + 全球加速 |
| **合计** | **0 元** |

只有当你的站点月流量超过 10GB 时才需要付费（按经验，内部小站几乎不可能达到）。

---

## 七、本地预览（可选）

部署前可在本地预览效果：

```bash
cd "粤东医疗设备招标信息汇总平台"
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

或用 npm 脚本：
```bash
npm run serve
```

测试抓取脚本（不影响生产数据，会写入新条目）：
```bash
node scripts/scrape.cjs
```

---

## 八、自定义域名（可选）

如果想用自己的域名（如 `tender.yourdomain.com`）：

1. 在 EdgeOne Pages 控制台 → 域名设置 → 添加自定义域名
2. 在你的域名 DNS 服务商添加 CNAME 记录指向 EdgeOne 提供的域名
3. EdgeOne 自动签发 SSL 证书（HTTPS 立即生效）

---

## 九、常见问题

### Q1：第一次部署后页面打不开？
A：在 EdgeOne Pages → 部署详情页查看构建日志。大概率是缓存，1-2 分钟后再试。

### Q2：抓取脚本抓不到新数据？
A：在 GitHub → Actions → Daily Data Update → 最新运行 → 查看日志。可能原因：
- ccgp/gdgpo 改了 HTML 结构（需要更新 `scripts/scrape.cjs` 的正则）
- 关键词没覆盖到（可在 `MED_KEYWORDS` 数组中添加）

### Q3：如何修改抓取频率？
A：编辑 `.github/workflows/daily-update.yml` 中的 cron 表达式。例如改为每天 3 次（9:00/13:00/17:00）：
```yaml
schedule:
  - cron: '0 1 * * *'   # 北京 9:00
  - cron: '0 5 * * *'   # 北京 13:00
  - cron: '0 9 * * *'   # 北京 17:00
```

### Q4：如何添加新的数据源（如医院官网）？
A：在 `scripts/scrape.cjs` 中新增 `scrapeXXX()` 函数，加入主流程即可。模式：
```js
async function scrapeXXX() {
  const out = [];
  const html = await fetchUrl('https://example.com/list');
  // ... 解析 ...
  return out;
}
// 在 main() 中：raw = raw.concat(await scrapeXXX());
```

### Q5：如何彻底离线（不依赖 Tailwind CDN）？
A：可下载 Tailwind 到本地 `assets/`，修改 `index.html` 引用。要做的话告诉我，我可以帮你本地化。

### Q6：不需要 WorkBuddy 了，之前的自动化可以关掉吗？
A：可以。在 WorkBuddy 中暂停自动化 `automation-1785124432893`，GitHub Actions 会接管所有数据更新职责。

---

## 十、回滚方案（如需）

如果新方案出问题想回退：
1. 在 GitHub 仓库 → Actions → 历史运行 → 选择之前成功的 commit → Re-run
2. 或在 EdgeOne Pages → 部署历史 → 选择旧版本 → 「回滚到此版本」
3. 数据文件 `js/data.js` 始终在 Git 历史中保留，可随时回退

---

## 总结：你需要做的 3 件事

1. **注册 GitHub 账号**（2 分钟，免费）
2. **创建空仓库并推送代码**（2 分钟，按上面命令）
3. **在 EdgeOne Pages 创建应用并部署**（3 分钟，按提示点）

之后每天 9:00/15:00 自动运行，**永久免费**。