# 资深代码评审报告 — 粤东医疗设备招标信息汇总平台

> 评审人：Senior Developer（高级开发工程师）  
> 评审对象：`index.html` / `js/app.js` / `js/data.js` / `assets/styles.css`  
> 评审目标：代码质量把控 + 团队技术提升（以本项目为真实案例）  
> 结论：**功能可用，但存在多个「中级→高级」分水岭问题**。下面按严重级别列出，每一条都附带「为什么团队必须重视」。

---

## 一、总览评分卡

| 维度        | 现状  | 评级 | 说明                      |
| --------- | --- | -- | ----------------------- |
| 功能正确性     | 可用  | 🟢 | 搜索/筛选/分页/弹窗均工作          |
| 前端安全（XSS） | 有隐患 | 🔴 | 动态数据直接拼进 `innerHTML`    |
| 健壮性 / 容错  | 弱   | 🔴 | 单条脏数据可拖垮整页              |
| 作用域 / 模块化 | 弱   | 🟡 | 全部顶级 `var` 污染全局         |
| 可维护性      | 中   | 🟡 | 重复逻辑、魔法数字、分类法散落         |
| 无障碍（a11y） | 弱   | 🟡 | 输入框无 label、弹窗无焦点管理      |
| 性能        | 中   | 🟡 | 搜索未防抖、Tailwind CDN 生产误用 |
| 工程化 / 测试  | 缺   | 🔴 | 零测试、文档与代码漂移             |
| 文档        | 滞后  | 🟡 | README 数据量/频率已过时        |

---

## 二、问题清单（按严重级别）

### 🔴 Critical / High

#### H1. XSS 与渲染错乱：动态数据直接拼接 `innerHTML`

- **位置**：`app.js:109-119`（列表卡片）、`app.js:182-195`（弹窗正文）
- **现状**：
  ```js
  card.innerHTML = '<h3>…</h3>' + a.title + '…';   // 标题未转义
  body.innerHTML = '…本' + a.type + '由' + a.region + '…'; // 多个字段未转义
  ```
- **影响**：
  1. 若公告标题含 `<`、`&`、引号（政府公告站偶尔出现 `&`、全角引号本身安全，但 `<`/属性注入会破坏结构），页面会**渲染错乱**。
  2. 更关键：数据由**自动任务从外部站点抓取**写入，属于不可控第三方内容。一旦某标题含 `<img onerror=...>` 之类，就是**存储型 XSS**。这是面试和上线审查的硬性红线。
- **修复**：所有动态文本经 `escapeHtml()` 转义，或改用 `document.createElement` + `textContent` 构建 DOM。
- **对团队的意义**：「**永远不要把外部/动态数据直接拼进 HTML**」是前端安全的第一课，必须养成肌肉记忆。

#### H2. 零数据校验 → 单条脏数据拖垮整页

- **位置**：`app.js:34, 50-51`（`getFiltered` 排序）
- **现状**：排序逻辑 `b.publishDate.localeCompare(a.publishDate)` 假设每条都有合法 `publishDate`。`data.js` 由自动化**持续追加**，缺字段是必然会发生的事。
- **影响**：只要有一条 `publishDate` 为 `undefined`，`localeCompare` 抛异常 → 整个列表白屏。同样，`ANNOUNCEMENTS` 若加载失败，`init()` 在 `ANNOUNCEMENTS.length` 直接抛错。
- **修复**：加载即做 schema 校验 + 安全兜底（缺失字段给默认值、非法记录跳过并告警），脏数据不再传染全页。
- **对团队的意义**：**「输入不可信」不仅指用户，也包括自己写的自动化产物**。防御性编程是高级工程师的标配。

#### H3. 全局命名空间污染

- **位置**：`app.js` 与 `data.js` 全部顶级 `var`（`ANNOUNCEMENTS` / `CATEGORIES` / `state` / 所有函数）
- **影响**：在团队协作 / 多脚本合并场景下，全局变量极易冲突；函数名 `openModal`、`init` 等过于通用，撞名概率高。
- **修复**：`app.js` 整体包进 IIFE（`(function(){ … })();`），仅对外暴露必要接口。
- **对团队的意义**：**模块边界意识**——代码要能安全地与其他代码共存。

### 🟡 Medium

#### M1. 重复的重载逻辑

- **位置**：`reloadData()`（`app.js:215-228`）与刷新按钮回调（`app.js:250-274`）约 90% 相同。
- **修复**：统一为 `loadData(isManual, btn)`，两处复用。

#### M2. 分类法（taxonomy）硬编码且不一致

- **位置**：`app.js:8-9`、导航标签 vs 数据值
- **现状**：导航显示 `彩超/超声设备`，但匹配 `a.category === '彩超·超声设备'`；匹配靠「字符串相等」，数据一旦改名导航静默失效。
- **修复**：把 `REGIONS` / `TYPES` / 分类枚举集中为单一事实来源（常量），导航与校验共用。

#### M3. 搜索未防抖

- **位置**：`app.js:242` 每个按键都全量重渲染。
- **修复**：`input` 事件加 ~200ms 防抖。当前 57 条无感，但这是该教团队的**正确姿势**。

#### M4. Tailwind CDN 用于「生产」

- **位置**：`index.html:7` `<script src="https://cdn.tailwindcss.com">`
- **影响**：Tailwind 官方明确该 CDN **仅限开发**（浏览器内实时编译整个框架，体积大、有 FOUC、无法 purge）。对「premium / 生产」站点，应改用 Tailwind CLI/PostCSS 编译出精减 CSS。
- **对团队的意义**：**分清 dev 工具与 prod 产物**是工程成熟度标志。

#### M5. 无障碍（a11y）缺口

- **位置**：`index.html` 搜索框、弹窗
- **现状**：`<input>` 无 `<label>`（仅靠 placeholder）；弹窗无 `role="dialog"`/`aria-modal`、无焦点陷阱（Tab 会跑到弹窗外）、打开时自动刷新仍会重渲染打断阅读。
- **修复**：补 `label`/`aria-label`；弹窗加 `role="dialog" aria-modal` + 焦点陷阱 + 打开时暂停自动刷新 + Esc 关闭（Esc 已有）。

### 🟢 Low / 工程卫生

#### L1. 魔法数字

- `pageSize: 10`、`5*60*1000` 散落。应提取为 `PAGE_SIZE`、`AUTO_REFRESH_MS` 等命名常量。

#### L2. 文档与代码漂移（README drift）

- README 仍写「当前 32 条」「每日 09:00」，实际数据已 **57 条**、自动化 **09:00 & 15:00** 两次。文档是 PR 的一部分，必须同步。

#### L3. 零测试 / 无 CI 门禁

- 一个被自动化持续写入的数据文件，却没有校验「id 唯一 / 字段完整 / 日期格式 / 链接协议」的闸门。最该补的正是**数据质量门禁**（见 `tests/validate-data.js`）。

#### L4. 无版本控制规范

- 团队技术提升的基石是 **Git + PR 评审**。当前项目未见仓库与评审流程，应优先建立。

---

## 三、本次已落地的改进（对应上述条目）

| 条目                                    | 交付物                                                             |
| ------------------------------------- | --------------------------------------------------------------- |
| H1 / H2 / H3 / M1 / M2 / M3 / M5 / L1 | 重构版 `js/app.js`（IIFE + escapeHtml + 校验兜底 + 去重 + 常量化 + 防抖 + 无障碍） |
| H2 / L3                               | `tests/validate-data.js` 数据质量门禁（可接 CI/pre-commit）               |
| M4                                    | 规范文档中列为「生产化待办」，需后续引入构建步骤                                        |
| L2 / M5                               | `README.md` 修正 + `index.html` 无障碍属性补齐                           |
| L4                                    | `TEAM_STANDARDS.md` 中的评审清单与 DoD                                 |



---

## 四、给团队的一句话总结

> 这份代码「能跑」，但\*\*「能跑」和「能交付」之间，差的就是这几条红线\*\*：不信任任何输入（H1/H2）、守住模块边界（H3）、消除重复与魔法值（M1/M2/L1）、把无障碍和文档当功能来做（M5/L2）、给自动化产物上一道测试闸门（L3）。把这五条变成团队的 code review 清单，技术水平会在几周内肉眼可见地提升。
